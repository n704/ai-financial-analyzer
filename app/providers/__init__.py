"""Provider abstraction: LLM, embeddings, and vector-store protocols + factory.

Protocols land in P1.2 (``base.py``), the config-driven factory in P1.3
(``factory.py``). Only modules under this package may import vendor SDKs.
"""
