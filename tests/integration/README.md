# Integration tests

Service-level tests against a `FakeLLMProvider` (P1.4) and real Postgres/Redis via
testcontainers. Populated from P1.8 onward; the CI job that runs them is added with
the database layer.
